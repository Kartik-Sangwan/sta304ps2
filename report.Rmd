---
title: "report"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r}
# Real data from 2019 National Election
newfoundland_and_labrador<-read.csv("data/Newfoundland and Labrador.csv")HistNL <- ggplot(data=newfoundland_and_labrador, aes(x=party_name, y=votes)) + geom_bar(stat="identity", fill="steelblue") + theme_minimal()+ theme(axis.text.x = element_text(angle=65, vjust=0.6))
british_columbia<-read.csv("data/British Columbia.csv")HistBC <- ggplot(data=british_columbia, aes(x=party_name, y=votes)) + geom_bar(stat="identity", fill="steelblue") + theme_minimal()+ theme(axis.text.x = element_text(angle=65, vjust=0.6))
manitoba<-read.csv("data/Manitoba.csv")HistManitoba <- ggplot(data=manitoba, aes(x=party_name, y=votes)) + geom_bar(stat="identity", fill="steelblue") + theme_minimal()+ theme(axis.text.x = element_text(angle=65, vjust=0.6))
new_brunswick<-read.csv("data/New Brunswick.csv")HistBrunswick <- ggplot(data=new_brunswick, aes(x=party_name, y=votes)) + geom_bar(stat="identity", fill="steelblue") + theme_minimal()+ theme(axis.text.x = element_text(angle=65, vjust=0.6))
northwest_territories<-read.csv("data/Northwest Territories.csv")HistNorthwestTerritories <- ggplot(data=northwest_territories, aes(x=party_name, y=votes)) + geom_bar(stat="identity", fill="steelblue") + theme_minimal()+ theme(axis.text.x = element_text(angle=65, vjust=0.6))
nova_scotia<-read.csv("data/Nova Scotia.csv")HistNovaScotia <- ggplot(data=nova_scotia, aes(x=party_name, y=votes)) + geom_bar(stat="identity", fill="steelblue") + theme_minimal()+ theme(axis.text.x = element_text(angle=65, vjust=0.6))
nunavut<-read.csv("data/Nunavut.csv")HistNunavut <- ggplot(data=nunavut, aes(x=party_name, y=votes)) + geom_bar(stat="identity", fill="steelblue") + theme_minimal()+ theme(axis.text.x = element_text(angle=65, vjust=0.6))
ontario<-read.csv("data/Ontario.csv")HistOntario <- ggplot(data=ontario, aes(x=party_name, y=votes)) + geom_bar(stat="identity", fill="steelblue") + theme_minimal()+ theme(axis.text.x = element_text(angle=65, vjust=0.6))
prine_edward_island<-read.csv("data/Prince Edward Island.csv")HistPrinceIsland <- ggplot(data=prine_edward_island, aes(x=party_name, y=votes)) + geom_bar(stat="identity", fill="steelblue") + theme_minimal()+ theme(axis.text.x = element_text(angle=65, vjust=0.6))
quebec<-read.csv("data/Quebec.csv")HistQuebec <- ggplot(data=quebec, aes(x=party_name, y=votes)) + geom_bar(stat="identity", fill="steelblue") + theme_minimal()+ theme(axis.text.x = element_text(angle=65, vjust=0.6))
saskatchewan<-read.csv("data/Saskatchewan.csv")HistSaskatchewan <- ggplot(data=saskatchewan, aes(x=party_name, y=votes)) + geom_bar(stat="identity", fill="steelblue") + theme_minimal()+ theme(axis.text.x = element_text(angle=65, vjust=0.6))
yukon<-read.csv("data/Yukon.csv")HistYukon <- ggplot(data=yukon, aes(x=party_name, y=votes)) + geom_bar(stat="identity", fill="steelblue") + theme_minimal()+ theme(axis.text.x = element_text(angle=65, vjust=0.6))
alberta <- read.csv("data/Alberta.csv")HistAlberta <- ggplot(data=alberta, aes(x=party_name, y=votes)) + geom_bar(stat="identity", fill="steelblue") + theme_minimal()+ theme(axis.text.x = element_text(angle=65, vjust=0.6))
canada<-read.csv("data/Canada.csv")HistCanada <- ggplot(data=canada, aes(x=party_name, y=votes)) + geom_bar(stat="identity", fill="steelblue") + theme_minimal()+ theme(axis.text.x = element_text(angle=65, vjust=0.6))
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
